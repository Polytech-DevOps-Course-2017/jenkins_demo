pipeline {
    agent any

    stages {
        // stage('Build') {
        //     steps {
        //         // Well, it's python...
        //     }
        // }
        stage('Test') {
            steps {
                echo 'Testing..'
                sh '''
if [[ -d docker_demo ]]; then
    rm -rf docker_demo
else
    git clone https://github.com/Polytech-DevOps-Course-2017/docker_demo.git
fi
'''

                sh 'cd docker_demo; python3 -m unittest -v'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}

