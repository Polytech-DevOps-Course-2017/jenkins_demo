pipeline {
    agent any

    stages {
        // stage('Build') {
        //     steps {
        //         // Well, it's python...
        //     }
        // }
        stage('Test') {
            steps {
                echo 'Testing..'
                sh 'python3 -m unittest -v'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}

